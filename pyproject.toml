[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mpt-director-workflow"
version = "0.1.0"
description = "Director-layer workflow, QA gates, and local WebUI for MoneyPrinterTurbo video production."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "MPT Director Workflow Contributors" }
]
dependencies = []

[project.scripts]
mpt-director-webui = "director_workflow.webui:main"
